# Runtime dependency: the official Python MCP SDK.
mcp>=1.2,<2

# Optional: install the Python sibling tools so the shell-backed tools
# (normalize_bank_statement, classify_document) work out of the box.
# statement-normalizer>=0.1,<1.0
# doc-classifier-kit>=0.1,<1.0
